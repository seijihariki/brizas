#include "opengl.hpp"

#ifndef __SHADERS_HPP__
#define __SHADERS_HPP__

// Load shader given files of the vertex and fragment programs
GLuint LoadShaders(const char * vertex_file_path,const char * fragment_file_path);

#endif
